# from scipy import constants as constant
# import numpy as np

# cavity_frequency = 1.0 * 2 * np.pi
# atomic_frequency = 1.0 * 2 * np.pi
# coupling_strength = 0.05 * 2 * np.pi
# cavity_dissipation_rate = 0.005
# atom_dissipation_rate = 0.05
# no_of_cavity_states = 15
# avg_thermal_excitation = 0.0